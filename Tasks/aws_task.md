
1️⃣ *Install and configure Apache (httpd) and Nginx on the same Linux server.*
Ensure both web servers run on *different ports* and are *accessible from the browser*

2️⃣ *Set up a web server, create an AMI from the EC2 instance, and launch a new EC2 instance using that AMI.*

3️⃣ *Create a new EBS volume*, attach it to an EC2 instance, and *mount it on the server*.

4️⃣ *Perform disk partitioning* on a Linux system and *mount the partition permanently* using `/etc/fstab`.

5️⃣ *Configure Logical Volume Management (LVM):*

* Create Physical Volume (PV)
* Create Volume Group (VG)
* Create Logical Volume (LV)
* Format and mount the Logical Volume

6️⃣ *Create and configure swap space* (swap partition or swap file) on Linux.

7️⃣ *Create an EBS snapshot* and **restore a volume from the snapshot**.

8️⃣ *Set up Amazon EFS*, mount it on an EC2 instance, and verify access.

9️⃣ *Recover a lost EC2 key pair* and **regain SSH access** to the instance.

